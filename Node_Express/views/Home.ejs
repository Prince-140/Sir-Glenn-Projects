<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Monitoring App - Welcome</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        /* Reset and Base Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        :root {
            --primary-color: #1a365d;
            --secondary-color: #ed8936;
            --accent-color: #4299e1;
            --background-color: #f7fafc;
            --text-color: #2d3748;
            --light-gray: #e2e8f0;
            --white: #ffffff;
            --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
            --transition: all 0.3s ease;
        }
        
        body {
            font-family: 'Inter', sans-serif;
            line-height: 1.6;
            color: var(--text-color);
            background-color: var(--background-color);
            overflow-x: hidden;
        }
        
        h1, h2, h3, h4 {
            font-family: 'Poppins', sans-serif;
            font-weight: 600;
            line-height: 1.3;
        }
        
        .container {
            width: 100%;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }
        
        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            padding: 14px 32px;
            border-radius: 8px;
            font-weight: 600;
            font-size: 1rem;
            text-decoration: none;
            cursor: pointer;
            transition: var(--transition);
            border: none;
            outline: none;
            font-family: 'Inter', sans-serif;
        }
        
        .btn-primary {
            background-color: var(--primary-color);
            color: var(--white);
        }
        
        .btn-primary:hover {
            background-color: #2d4a8a;
            transform: translateY(-2px);
            box-shadow: var(--shadow-lg);
        }
        
        .btn-secondary {
            background-color: var(--secondary-color);
            color: var(--white);
        }
        
        .btn-secondary:hover {
            background-color: #f6ad55;
            transform: translateY(-2px);
            box-shadow: var(--shadow-lg);
        }
        
        .btn-outline {
            background-color: transparent;
            color: var(--primary-color);
            border: 2px solid var(--primary-color);
        }
        
        .btn-outline:hover {
            background-color: var(--primary-color);
            color: var(--white);
            transform: translateY(-2px);
            box-shadow: var(--shadow-lg);
        }
        
        /* Header Styles */
        header {
            background-color: var(--white);
            box-shadow: var(--shadow);
            position: sticky;
            top: 0;
            z-index: 100;
        }
        
        .header-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 0;
        }
        
        .logo {
            display: flex;
            align-items: center;
            gap: 12px;
        }
        
        .logo-icon {
            width: 50px;
            height: 50px;
            background: linear-gradient(135deg, var(--primary-color), var(--accent-color));
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--white);
            font-size: 1.5rem;
        }
        
        .logo-text h1 {
            font-size: 1.5rem;
            color: var(--primary-color);
            margin-bottom: 4px;
        }
        
        .logo-text p {
            font-size: 0.8rem;
            color: var(--text-color);
            opacity: 0.8;
        }
        
        .tagline {
            font-size: 0.9rem;
            color: var(--text-color);
            opacity: 0.8;
            display: none;
        }
        
        /* Hero Section */
        .hero {
            padding: 80px 0;
            background: linear-gradient(to bottom, var(--white) 0%, var(--background-color) 100%);
        }
        
        .hero-content {
            display: grid;
            grid-template-columns: 1fr;
            gap: 60px;
            align-items: center;
        }
        
        @media (min-width: 992px) {
            .hero-content {
                grid-template-columns: 1fr 1fr;
            }
        }
        
        .hero-text h2 {
            font-size: 2.5rem;
            color: var(--primary-color);
            margin-bottom: 20px;
        }
        
        .hero-text p {
            font-size: 1.1rem;
            margin-bottom: 30px;
            color: var(--text-color);
            opacity: 0.9;
        }
        
        .hero-image {
            position: relative;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        
        .dashboard-preview {
            width: 100%;
            max-width: 500px;
            background: var(--white);
            border-radius: 16px;
            padding: 30px;
            box-shadow: var(--shadow-lg);
            position: relative;
            overflow: hidden;
        }
        
        .dashboard-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }
        
        .dashboard-title {
            font-weight: 600;
            color: var(--primary-color);
        }
        
        .dashboard-content {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 15px;
            margin-bottom: 20px;
        }
        
        .dashboard-item {
            background: var(--light-gray);
            border-radius: 8px;
            padding: 15px;
            text-align: center;
        }
        
        .dashboard-item i {
            font-size: 1.5rem;
            color: var(--primary-color);
            margin-bottom: 10px;
        }
        
        .dashboard-item span {
            display: block;
            font-size: 0.8rem;
            font-weight: 500;
        }
        
        .device-mockups {
            position: absolute;
            bottom: -20px;
            right: -20px;
            display: flex;
            gap: 10px;
        }
        
        .device {
            width: 80px;
            height: 120px;
            background: var(--primary-color);
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--white);
            font-size: 1.5rem;
            box-shadow: var(--shadow);
        }
        
        .device.tablet {
            width: 100px;
            height: 140px;
        }
        
        /* Features Section */
        .features {
            padding: 80px 0;
            background-color: var(--white);
        }
        
        .section-title {
            text-align: center;
            margin-bottom: 50px;
        }
        
        .section-title h3 {
            font-size: 2rem;
            color: var(--primary-color);
            margin-bottom: 15px;
        }
        
        .section-title p {
            color: var(--text-color);
            opacity: 0.8;
            max-width: 600px;
            margin: 0 auto;
        }
        
        .features-grid {
            display: grid;
            grid-template-columns: 1fr;
            gap: 30px;
        }
        
        @media (min-width: 768px) {
            .features-grid {
                grid-template-columns: repeat(3, 1fr);
            }
        }
        
        .feature-card {
            background: var(--background-color);
            border-radius: 12px;
            padding: 30px;
            text-align: center;
            transition: var(--transition);
            box-shadow: var(--shadow);
        }
        
        .feature-card:hover {
            transform: translateY(-5px);
            box-shadow: var(--shadow-lg);
        }
        
        .feature-icon {
            width: 70px;
            height: 70px;
            background: linear-gradient(135deg, var(--primary-color), var(--accent-color));
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 20px;
            color: var(--white);
            font-size: 1.8rem;
        }
        
        .feature-card h4 {
            font-size: 1.3rem;
            margin-bottom: 15px;
            color: var(--primary-color);
        }
        
        .feature-card p {
            color: var(--text-color);
            opacity: 0.9;
        }
        
        /* Authentication Section */
        .auth-section {
            padding: 80px 0;
            background-color: var(--background-color);
            text-align: center;
        }
        
        .auth-content {
            max-width: 600px;
            margin: 0 auto;
        }
        
        .auth-content h3 {
            font-size: 2rem;
            color: var(--primary-color);
            margin-bottom: 15px;
        }
        
        .auth-content p {
            color: var(--text-color);
            opacity: 0.9;
            margin-bottom: 30px;
        }
        
        .auth-buttons {
            display: flex;
            flex-direction: column;
            gap: 15px;
            margin-bottom: 30px;
        }
        
        @media (min-width: 576px) {
            .auth-buttons {
                flex-direction: row;
                justify-content: center;
            }
        }
        
        .auth-links {
            display: flex;
            flex-direction: column;
            gap: 10px;
        }
        
        @media (min-width: 576px) {
            .auth-links {
                flex-direction: row;
                justify-content: center;
                gap: 20px;
            }
        }
        
        .auth-links a {
            color: var(--primary-color);
            text-decoration: none;
            font-weight: 500;
            transition: var(--transition);
        }
        
        .auth-links a:hover {
            color: var(--accent-color);
            text-decoration: underline;
        }
        
        /* Footer */
        footer {
            background-color: var(--primary-color);
            color: var(--white);
            padding: 40px 0;
        }
        
        .footer-content {
            text-align: center;
        }
        
        .privacy-statement {
            margin-bottom: 20px;
            font-size: 0.9rem;
            opacity: 0.9;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
        }
        
        .copyright {
            font-size: 0.8rem;
            opacity: 0.7;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            padding-top: 20px;
        }
        
        /* Responsive Adjustments */
        @media (max-width: 768px) {
            .hero-text h2 {
                font-size: 2rem;
            }
            
            .hero {
                padding: 60px 0;
            }
            
            .features, .auth-section {
                padding: 60px 0;
            }
            
            .section-title h3 {
                font-size: 1.8rem;
            }
        }
        
        @media (max-width: 576px) {
            .header-content {
                flex-direction: column;
                gap: 15px;
                text-align: center;
            }
            
            .tagline {
                display: block;
            }
            
            .hero-text h2 {
                font-size: 1.8rem;
            }
            
            .dashboard-preview {
                padding: 20px;
            }
            
            .dashboard-content {
                grid-template-columns: 1fr;
            }
            
            .feature-card {
                padding: 25px 20px;
            }
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header>
        <div class="container">
            <div class="header-content">
                <div class="logo">
                    <div class="logo-icon">
                        <i class="fas fa-home-shield"></i>
                    </div>
                    <div class="logo-text">
                        <h1>Home Monitoring App</h1>
                        <p>Security and Peace of Mind</p>
                    </div>
                </div>
                <div class="tagline">
                    Real-time security monitoring for your home
                </div>
            </div>
        </div>
    </header>

    <!-- Hero Section -->
    <section class="hero">
        <div class="container">
            <div class="hero-content">
                <div class="hero-text">
                    <h2>Monitor Your Home, Anytime, Anywhere</h2>
                    <p>Real-time security monitoring, smart alerts, and complete home oversight from your devices. Protect what matters most with our advanced home monitoring solution.</p>
                    <div class="auth-buttons">
                        <a href="/Login" class="btn btn-primary">Log In</a>
                        <a href="/SignUp" class="btn btn-secondary">Sign Up</a>
                    </div>
                </div>
                <div class="hero-image">
                    <div class="dashboard-preview">
                        <div class="dashboard-header">
                            <div class="dashboard-title">Home Dashboard</div>
                            <div class="status-indicator">
                                <span style="color: #48bb78; font-size: 0.9rem;">
                                    <i class="fas fa-circle" style="font-size: 0.7rem;"></i> All Systems Active
                                </span>
                            </div>
                        </div>
                        <div class="dashboard-content">
                            <div class="dashboard-item">
                                <i class="fas fa-video"></i>
                                <span>Live Cameras</span>
                            </div>
                            <div class="dashboard-item">
                                <i class="fas fa-shield-alt"></i>
                                <span>Security Status</span>
                            </div>
                            <div class="dashboard-item">
                                <i class="fas fa-temperature-low"></i>
                                <span>Climate Control</span>
                            </div>
                            <div class="dashboard-item">
                                <i class="fas fa-lightbulb"></i>
                                <span>Lighting</span>
                            </div>
                        </div>
                        <div class="device-mockups">
                            <div class="device phone">
                                <i class="fas fa-mobile-alt"></i>
                            </div>
                            <div class="device tablet">
                                <i class="fas fa-tablet-alt"></i>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Features Section -->
    <section class="features">
        <div class="container">
            <div class="section-title">
                <h3>Advanced Home Security Features</h3>
                <p>Our comprehensive monitoring system provides everything you need for complete home security and peace of mind.</p>
            </div>
            <div class="features-grid">
                <div class="feature-card">
                    <div class="feature-icon">
                        <i class="fas fa-video"></i>
                    </div>
                    <h4>Live Video Monitoring</h4>
                    <p>Access real-time video feeds from your home security cameras with AI-powered motion and facial detection.</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">
                        <i class="fas fa-bell"></i>
                    </div>
                    <h4>Smart Alert System</h4>
                    <p>Receive instant notifications for unusual activities, security breaches, or system alerts directly to your devices.</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">
                        <i class="fas fa-laptop-house"></i>
                    </div>
                    <h4>Multi-Device Access</h4>
                    <p>Monitor and control your home security from any device - smartphone, tablet, or desktop computer.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Authentication Section -->
    <section class="auth-section">
        <div class="container">
            <div class="auth-content">
                <h3>Get Started with Your Secure Home</h3>
                <p>Join thousands of homeowners who trust our monitoring system to protect their homes and families.</p>
                
                <div class="auth-buttons">
                    <a href="/" class="btn btn-primary">Log In</a>
                    <a href="/SignUp" class="btn btn-secondary">Sign Up</a>
                </div>
                
                <div class="auth-links">
                    <a href="/">Already have an account? Log in</a>
                    <a href="/SignUp">New to Home Monitoring? Create your account</a>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="container">
            <div class="footer-content">
                <p class="privacy-statement">Your security is our priority. All data is encrypted and protected with enterprise-grade security measures.</p>
                <p class="copyright">&copy; 2024 Home Monitoring App. All rights reserved.</p>
            </div>
        </div>
    </footer>

    <script>
        // Simple hover effect for feature cards
        document.addEventListener('DOMContentLoaded', function() {
            const featureCards = document.querySelectorAll('.feature-card');
            
            featureCards.forEach(card => {
                card.addEventListener('mouseenter', function() {
                    this.style.transform = 'translateY(-5px)';
                });
                
                card.addEventListener('mouseleave', function() {
                    this.style.transform = 'translateY(0)';
                });
            });
            
            // Smooth scrolling for anchor links (if any are added later)
            document.querySelectorAll('a[href^="#"]').forEach(anchor => {
                anchor.addEventListener('click', function (e) {
                    e.preventDefault();
                    const targetId = this.getAttribute('href');
                    if(targetId !== '#') {
                        const targetElement = document.querySelector(targetId);
                        if(targetElement) {
                            window.scrollTo({
                                top: targetElement.offsetTop - 80,
                                behavior: 'smooth'
                            });
                        }
                    }
                });
            });
        });
    </script>
</body>
</html>